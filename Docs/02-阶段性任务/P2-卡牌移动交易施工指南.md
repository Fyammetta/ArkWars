# P2 施工指南：卡牌移动交易

> 对应整体卷：`01-整体设计/04-交易模型`、`08-卡牌与容器模型`（并承接 `P0` §3 分派项）。

> 目标：落地 `UBattleTransaction`/`UCardMoveTransaction`，两容器按 `08` §3 定稿读写接口
> （`Consume`/`Add`）真正落子，让 `FMoveMessage` 描述的任何基础移动在服务器正确执行、客户端可见。
>
> 本阶段**不接时机窗口、不写阶段体规则**（时机窗口 P3 起，阶段体规则属后续薄层施工，见 §5 边界）；
> `QueryTimings`/`OnWindowClosed` 等 P3 方法**占位先行**（状态已写、开窗逻辑留 P3），
> 同步驱动与 while 清空行为等价。

> **实施状态：⚠ 进行中（2026-09-09 复核，当日最新代码再对）。**
>
> **已就绪（09-08）**：容器接口声明与交易基类五态 + 单收尾。
>
> **已落（09-09）**：`UCardMoveTransaction` 族实现——`Execute` 执行骨架 + `Validate`
> + `FoldMods` 直通空实现；`BroadcastFinish(bool)` 仍空、族通知未广播。
>
> **队列驱动体：已修正通（09-09 当日）**——`EnqueueTransaction` 已把 `Tx` 真正加入
> `PendingTransactions`（忙锁时排队后 `return`）；`DriveTransaction` 已加空队列守卫 + `RemoveAt`；
> 收尾踢链 `OnTransactionFinished` 完整（Active 校验 → 清 Active → 再 Drive）。
> 缺陷①②③已消除（见 §2-C）。
>
> **两容器写接口：已实现、缺陷待修（09-09 复核修正，见 §2-E）**——六方法两端均已落；玩家侧
> `Consume`/`Add` 已有 `HasAuthority` 门控、**桌面侧未建**；剩余缺陷：`Consume` 源区删除
> 下标错位、玩家 `Add` Equipment/Judgement 不落区（细目 §2-E）；`StartGame` 已接入
> `ModeComp->InitCardDeck()` 调用，但 `InitCardDeck`（Default/Factions）覆写仍空。
>
> **当前阻塞：§2-E 写接口缺陷修复 + `InitCardDeck` 真填牌堆（§2 E/G）——队列驱动已修正（§2-C）。**

> **接口对齐注（2026-09-08，最新复核）**：
>
> 代码容器接口 = 读 `GetCardsByKey`/`GetCardById`/`Select`（DSL→实体 Id）/`GetAreaKeys`，
> 写 `Consume`（`[[nodiscard]]`，**已去 `const`**，按 Id 整壳取）/`Add`（`EAreaWriteResult`，
> 按 `_Order/_bDiscard` 落位），另增 `GetContainerActor()`（两容器均已内联实现）。
>
> 本节下表已按真实代码重核。**`UCardMoveTransaction::Cards` 已桥接 `FArkCard`
> （`TArray<FArkCard>`，卷 `08` §7.1/§7.5，不再存"裸标签载荷未桥接"差异，09-09 复核）**；
> 移动主链现含 `HandleMovement*` 双路径与同区短路（§2-D 按现代码重述）。遗留偏差收口为：
> `BroadcastFinish` 族广播空、`SetCardOrder` 废弃残留（同区整理交 UI，09-09 决策，§2-D）、
> `ResolveContainer` 公共入口未抽。
> D/E 落子、验收与检查表均按新接口执行。

## 1. 现有零件盘点（2026-09-08 复核，09-09 复查）

- **`UBattleTransaction`** — ✅ 骨架已落地并已按 §2 **五态 + 单收尾**重构
  - **位置**：`Battle/Transaction/BattleTransaction.h/.cpp`、`Battle/Transaction/EventTypes.h`
  - **要点**：
    - 生命周期状态 = 基类内嵌 `enum EState { Spawned, Started, Querying, Executed, Finished }`；
    - `Start()`（公开）：服务器门控 → `protected virtual bool Validate() const` 族校验钩子
      + `State != Spawned` 防重入 → `Started` → `EnqueueTransaction(this)`；
    - `QueryTimings()`（置 `Querying`）/ `OnWindowClosed()`（空）为 P3 占位；
    - `Execute()` 公开纯虚（09-09 核实声明于 `public`，驱动器直调可行；族实现、自收尾 `Finish`）；
    - `Finish(bool bSuccess)` 唯一收尾：落 `Finished` → 族钩子 `BroadcastFinish(bool)`
      → 子系统 `OnTransactionFinished` 踢链（`.cpp` 已核）；
    - `Begin`/`Cancel`/`bBegun`/`End`/`NotifyTimings`/`Init`/`GetInstigator`/
      `GetTarget`/`IsTransactionCanceled` 全删净；
    - 上下文公开字段：`Instigator` = `TWeakObjectPtr<AActor>`、
      `Targets` = `TArray<TWeakObjectPtr<AActor>>`；
    - `AppendModification(const FTransactionModRequest&)`（限 `State==Querying`）**已落地**；
      `FoldMods()` 公开纯虚（族必须实现）+ `protected State`/`ModeRequests`；
    - 折叠载体 `FTransactionModRequest` + Op 集 `EModOp{Ignore, Add, Multiply, Clamp}`
      定义于 `EventTypes.h`（09-08 拍板收敛）。

- **`UCardMoveTransaction`** — ⚠ 独立文件；族实现**已落（09-09）**
  - **位置**：`Battle/Transaction/SubTransaction/CardMoveTransaction.*`
  - **要点**：
    - `Create(Msg, CardsToMove = {})`：解析 → 持有 `Message` 意图；`CardsToMove` 预填
      `Cards`（显式指定牌路径）；
    - `Execute()`：`Validate` → 双端 `GetInterface` → `FoldMods` →
      `Cards` 空 → `HandleMovement`（`Select` → `Consume`）；非空 →
      `HandleMovement_Selected`（`GetCardsByKey` 定位 → `Consume`）→ `To->Add`，
      按 `EAreaWriteResult` 收尾 `Finish(true/false)`；`From == To && _From == _To`
      同区短路 `Finish(false)`；
    - `Validate()`：Instigator / 单 Target 带容器接口 + `Message->IsValid()`
      （失败日志空指针防御已落，不再对无效对象 `->GetName()` 解引用）；
    - `FoldMods()` 直通空实现；`BroadcastFinish(bool)` **仍空**
      （族广播 `Notify::Card::Moved` 待填）；
    - `Cards` 已桥接 `FArkCard`（不再存"裸标签载荷残留"，09-09）；遗留 =
      `BroadcastFinish` 广播空、`SetCardOrder` 废弃残留待删（同区整理交 UI，09-09，§2-D）、
      `bIsSelected` 分支方向观察项（§2-D/§7）。

- **`ICardContainerInterface`** — ✅ 已按 `08` §3 定型并扩至七方法
  - **位置**：`Battle/Toolkits/CardContainerInterface.h`
  - **要点**：
    - 读：`GetCardsByKey`/`GetCardById`/`Select`/`GetAreaKeys`；
    - 写：`Consume`（**已去 `const`**）/`Add`；另增 `GetContainerActor()`；
    - `MoveIn`/`MoveOut` 0 残留。

- **`UCardManagementBusComponent`** — ⚠ 写接口已实现、缺陷待修
  - **位置**：`Battle/Component/Card/CardManagementBusComponent.*`
  - **要点**：
    - `HandCards`/`EquipmentArea`/`JudgementArea` 复制就绪；
    - `GetAreaKeys`/`GetCardsByKey`（含跨容器委托）已实现，`GetContainerActor()` 内联（返 `Owner`）；
    - `GetCardById`/`Select` 已实现；`Consume`/`Add` 已实现且带 `HasAuthority` 门控，但
      `Consume` 源区删除下标错位、`Add` Equipment/Judgement 分支经空实现 `Equip`/
      `PutIntoJudgement` 不落区只清参（09-09 复核修正，细目 §2-E）。

- **`ACardTableManager`** — ⚠ 写接口已实现、缺陷待修
  - **位置**：`Battle/CardTableManager.*`
  - **要点**：
    - 世界单例；Pile/Discard/Cache/Used/`JudgementArea` 五字段均已 `DOREPLIFETIME` + 独立
      OnRep（`08` §2 判定区归玩家侧，桌面 `JudgementArea` 语义/去留属观察项 §2-E/§7）；
    - `GetCardsByKey`/`GetAreaKeys`（Pile/Discard/Cache/Used 四区，**不含 Judgement**）已实现，
      `GetContainerActor()` 内联（返 `this`）；
    - `GetCardById`/`Select`/`Consume`/`Add`（触达含 Judgement）已实现；`Consume` 源区删除
      下标错位、**桌面写接口无 `HasAuthority` 门控**（玩家侧已有）待修（09-09 复核修正，
      细目 §2-E）。

- **`UBattleGameFlowSubsystem`（交易承载）** — ✅ 驱动已修正通（09-09 当日）
  - **位置**：`Battle/System/BattleGameFlowSubsystem.*`
  - **要点**：
    - `PendingTransactions` + `ActiveTransaction`（`TWeakObjectPtr<UBattleTransaction>` 忙锁）；
    - `EnqueueTransaction`：`!Tx` 防御 → **`PendingTransactions.Add(Tx)` 真正入队** →
      `Active` 忙锁时排队 `return`、空闲才 `DriveTransaction`；
    - `DriveTransaction`：**`IsEmpty` 空队列守卫** + `RemoveAt(0)` 后 `Execute`；
    - `OnTransactionFinished`：Active==Tx 校验 → 清 Active → 再 Drive（基类 `Finish` 内已调用 ✔）；
    - `StartGame` 已接 `ModeComp->InitCardDeck()` 调用（选角通知后）+ 逐玩家 `Create`
      → 公开字段直赋 → `Start()`；实际仍不落子（§E 落子缺陷 + 牌堆空，见 §2 E/G）。

- **`FMoveMessage`** — ⚠ DSL 解析主体已实现
  - **位置**：`Battle/Toolkits/GameMessage.*`
  - **要点**：
    - 区域缩写 / `ORDER` / `AREA=` / `CONDITION` 谓词栈 / `_Meta`；
    - `_bDiscard` 判真缺陷**已修**（09-08：`GameMessage.cpp:159` 改与 FROM/TO 同源的键解析
      `FindKeyHelper`——`Find(IsDiscard) && Atoi(*Temp)`，无 DISCARD 键即短路 `false`、
      命中后值独立读取，不再 `-1` 恒真 / 读残留）；
    - `AREA=` 分支现经 `RequestGameplayTag` 走原生标签；运行回归并入 §3/§4。

- **`FCardAreaSlot` / Factory** — ✅ 槽基元 + 三 `Create` 完整实现；⚠ 全库零使用方
  - **位置**：`Battle/Toolkits/ArkWarCardTypes.*`
  - **要点**：区字段已直存 `TArray<FArkCard>`；槽基元去留由落子实现定稿。

- **区域标签** — ✅ 原生 GameplayTag（`Card.Area.Pile/Discard/Hand/Judgement/Equipment/Cache/Used`）
  - **位置**：`Battle/Toolkits/ArkWarTags.*`

区域归属约定见卷 `08` §2（玩家侧 Hand/Equipment/Judgement；桌面侧 Pile/Discard/Cache/Used/CardSource）。

## 2. 行动项（施工顺序即依赖序）

行动项状态速览（细目见各节；复核日期 09-09）：

| 项 | 内容 | 状态 |
|---|---|---|
| A | DSL 前置修复 | ✅ 已完成（回归并入 §3/§4） |
| B | 代码对齐（蓝图 vs 现状） | ✅ 已核销；尾项随 C/F（`Execute` 访问层级、`ResolveContainer` 类型链） |
| C | 交易基类与队列 | ✅ 已落地；驱动"单条 + 收尾踢"修正通（剩 Execute 访问层级决策点） |
| D | `UCardMoveTransaction` 族实现 | ⚠ 主体已落；广播待填、`SetCardOrder` 残留待删（Cards 桥接/崩溃防御/同区决策已收） |
| E | 两容器落子（写接口） | ⚠ 六方法两端已实现；玩家门控已加、桌面缺；`Consume` 删位错位与玩家 `Add` Equipment/Judgement 待修（09-09 复核修正） |
| F | `ResolveContainer` 统一入口 | ⚠ 局部落地（交易内 `GetInterface`）；公共入口未抽 |
| G | `StartGame` 发牌打通 | ❌ 未打通（`InitCardDeck` 空 + E 缺陷挡路） |

### A. DSL 前置修复（`GameMessage.cpp`，卷 `08` §5 纪律）

**状态：✅ 已完成（09-08 落地）。** 三项随本轮代码落位，运行回归并入 §3/§4 用例。

- [x] **`_bDiscard` 判真改键解析**：`GameMessage.cpp:159` 改用与 FROM/TO 同源的
  `FindKeyHelper`（`Find(IsDiscard) && Atoi(*Temp)`）短路——无 DISCARD 键即 `false`。
  原缺陷：`&&` 直用返回值，无 DISCARD 时 `-1` 恒 truthy、以 `DISCARD` 开头时 `0` 反为 false。
- [x] **DISCARD 值独立解析**：不复用 `Temp`（读的是 NUM/FROM/TO 残留：含 FROM/TO 时是区域码，
  `Atoi` 恒 0）；键解析短路后命中再独立取值（`Atoi(*T) != 0`），不再读残留。
- [x] **`AREA=` 特殊区分支确走原生标签**：现为 `_From/_To = FGameplayTag::RequestGameplayTag(...)`
  （P0 §3 余留 1 的 P2 侧确认）。

### B. 代码对齐（卷 `04` 蓝图 vs 现状偏差，先对齐再填实现）

**状态：✅ 已核销（09-08 逐条）；** 仅剩尾项随 C/F 走。每条 = 一个对齐项
（蓝图 → 现状 → 处置），已对齐的直接勾销：

- [x] **文件归属**：蓝图 = 文件在 `Battle/Transaction/`、移动交易独立于基类；
  现状 = 已分文件（`Transaction/SubTransaction/CardMoveTransaction.*`）。处置 = 已对齐，不动。
- [x] **生命周期入口**：蓝图 = `Start/Finish(bool)` 入口 + 族钩子四件（`04` §2 五态：
  无 `End`、无 `Cancel`）；现状 = 已按五态重构（`Start()`/`Finish(bool)`/
  `BroadcastFinish(bool)` 落地；`Begin`/`Cancel`/`End` 零残留）。处置 = 已对齐。
  - [ ] 尾项：`Execute` 现为公开、驱动器可直调（09-09）——访问去留见 §C 决策点。
- [x] **`Instigator/Target` 公开入参**：蓝图 = 公开入参；现状 = `Init(AActor*,
  const TArray<AActor*>&)` 已删净（09-08 最新），上下文改公开字段直赋：
  `Tx->Instigator = 容器Actor（Manager->GetContainerActor()）`、`Tx->Targets.Add(Player)`。
  处置 = 已对齐。
- [x] **Getter 返回 `APlayerState*`**：现状 = `GetInstigator/GetTarget` 已删净，上下文经公开
  `Instigator`/`Targets`（`TWeakObjectPtr<AActor>`）承载。
  - [ ] 尾项：`ResolveContainer` 签名类型链按 `AActor*` 在 F 步定稿。
- [x] **`DriveTransaction/OnTransactionFinished`**：蓝图 = 队列驱动方法分工；现状 = 已拆三方法
  （`Enqueue/Drive/OnTransactionFinished`），`ProcessTransaction` 已删；09-09 当日已按 §C
  修正方向收口：`Enqueue` 入队 + 忙锁排队、`Drive` 空守卫、收尾踢链。
  - [x] 尾项：驱动体按 §C 修正方向落地（缺陷①②③已消除，09-09）。
- [x] **P3 占位方法**：现状 = 收口为 `QueryTimings`（置 `Querying`）/`OnWindowClosed`
  （空，P3 填），`NotifyTimings` 已删。处置 = 已对齐（五态下无 Executed/Cancel 直写分支）。
- [x] **`GetState() const`**：蓝图 = 有；现状 = 已实现（内联返回 `EState`；
  `IsTransactionCanceled` 已删）。处置 = 已对齐。
- [x] **`ActiveTransaction`（指针，忙锁）**：现状 = 已改
  `TWeakObjectPtr<UBattleTransaction> ActiveTransaction`（`ActiveTransactionIndex` 零残留）。
  处置 = 已对齐（09-08 最新）。
- [x] **`Finish(bool bSuccess)` 唯一收尾**：蓝图 = 被防/无效 = `Finish(false)`
  （不 Execute，族钩子 `BroadcastFinish(false)` 广播防止通知）；现状 = 已落地
  （`Begin`/`Cancel`/`bBegun` 删净）。处置 = 已对齐，族广播待各族实现。
- [x] **基类收敛三件（`04` §4.1）`ModeRequests`/`AppendModification`/`FoldMods`**：
  折叠载体定稿于 `EventTypes.h`：`FTransactionModRequest` + `EModOp{Ignore, Add, Multiply,
  Clamp}`（减 = `Add` 负值、除 = `Multiply` 倒数，09-08 收敛），命名与卷 `04` **已统一**；
  基类 `protected ModeRequests` + 公开 `AppendModification()`（**已落地**，限 `State==Querying`）
  + 公开纯虚 `FoldMods()`（族必须实现）。
  - [ ] 尾项：`FoldMods` 折叠公式实现归 P4 伤害族（移动族已给直通）。
- [x] **`Consume` 签名**：已 `[[nodiscard]]`，`const` 已去掉（两容器签名同步）。处置 = 已对齐
  （09-08 最新）。

### C. 交易基类与队列（卷 `04` §2/§3/§7）

**状态：✅ 主体已落地；队列"单条驱动 + 收尾踢"已修正通（09-09 当日）；剩 `Execute` 访问层级决策点。**

已完成（09-08/09 代码复核确认，不再是施工目标）：

- [x] **生命周期按 `04` §2 五态 + 单收尾重构落地**
  - `Start()` 非虚三层：① 服务器门控（`GetNetMode` 检查）→ ② 调族钩子
    `protected virtual bool Validate() const`（基类 `PURE_VIRTUAL`，族覆写做必填校验，
    失败拒绝入队、可修复后重试）→ ③ `State != Spawned` 防重入 + `Started` +
    `EnqueueTransaction(this)`；
  - `Execute()` 为**公开**纯虚（09-09 核实：声明于基类 `public`，驱动器直调可编译；
    族实现、自收尾 `Finish(bool)`）；
  - `Finish(bool bSuccess)` 唯一收尾：落 `Finished` → 族钩子 `BroadcastFinish(bool)`
    → 子系统 `OnTransactionFinished` 踢链（`.cpp` 实现已核）；
  - `QueryTimings()`（置 `Querying`）/ `OnWindowClosed()`（空）为 P3 占位；
  - `Begin`/`Cancel`/`bBegun`/`NotifyTimings` 已删净。
- [x] **基类收敛三件随折叠载体定稿落地**（`04` §4.1/§4）
  - `EventTypes.h` 已定义折叠请求 `FTransactionModRequest`
    （`Operation`/`float Magnitude`/`Actor`/`Priority`）+ Op 集
    `EModOp{Ignore, Add, Multiply, Clamp}`（09-08 收敛：减 = `Add` 负值、除 = `Multiply` 倒数）；
  - 基类 `protected TArray<FTransactionModRequest> ModeRequests` + 公开
    `AppendModification(const FTransactionModRequest&)`（**已落地**：`State == Querying` 才收，
    P3 前无窗口 = 恒拒绝）+ 公开纯虚 `FoldMods()`（族必须实现：移动族无折叠 = 直通实现；
    伤害族 P4 给 `04` §4 三步折叠）。
- [x] **驱动体按"单条驱动 + 收尾踢"修正通（09-09 当日）**：`Enqueue/Drive/OnTransactionFinished`
  三方法按修正方向收口（缺陷①②③消除）——`Enqueue` 真正入队 + 忙锁排队、
  `Drive` 空守卫 + `RemoveAt`、收尾踢链（实现见下框；单条驱动形态 P2 即用，与 P3 同构，
  P3 只改一行 `Execute` → `QueryTimings`）：

```cpp
// 代码现状（BattleGameFlowSubsystem.cpp，09-09 修正后）：
void UBattleGameFlowSubsystem::EnqueueTransaction(UBattleTransaction* Tx) {
    if (!IsRunningOnServer()) return;          // 服务器门控
    if (!Tx) return;
    PendingTransactions.Add(Tx);               // ✔ 缺陷②消除：Tx 真正入队
    if (ActiveTransaction.IsValid()) return;   // ✔ 忙锁排队：Execute 期新 Enqueue 只入队、不递归驱动
    DriveTransaction();
}
void UBattleGameFlowSubsystem::DriveTransaction() {
    if (!IsRunningOnServer()) return;
    if (PendingTransactions.IsEmpty()) return; // ✔ 缺陷③消除：空队列守卫，首发不再越界
    ActiveTransaction = PendingTransactions[0];
    PendingTransactions.RemoveAt(0);
    ActiveTransaction->Execute();              // Execute 现为基类 public，驱动直调可行（09-09）
}
void UBattleGameFlowSubsystem::OnTransactionFinished(UBattleTransaction* Tx) {
    if (!IsRunningOnServer()) return;
    if (ActiveTransaction != Tx) return;       // 收尾交易须为 Active，防串扰
    ActiveTransaction = nullptr;
    DriveTransaction();                        // ✔ 收尾踢链：自然驱动下一条
}
```

忙锁语义（卷 `04` §3）由此实现兑现：`Execute` 内新 `Enqueue` 只入队不驱动；收尾回调踢到
下一条（"不递归、排队执行"）。

待办（决策点）：

- [ ] **决策点（09-09 更新）**：`Execute` 现为基类 `public`（`DriveTransaction` 直调
  `Active->Execute()` 已可编译，原"先开放入口"不构成障碍）；`QueryTimings`/`OnWindowClosed`/
  `Finish`/`Validate`/`BroadcastFinish` 仍 `protected`（P3 驱动器改调 `QueryTimings` 时仍面临
  入口问题）。待定：维持 `Execute` 公开（偏离 `04` §2"族钩子受保护"目标）或改回受保护 +
  增公开驱动入口——方案回填本卷 §7 与 `04` §7/附录注记。

约束（非行动项）：队列方法入口 `IsRunningOnServer()` 门控、客户端不驱动；`GetState()` 供调试。

### D. `UCardMoveTransaction`（卷 `04` §4）——族实现已落（09-09 复核代码）

**状态：⚠ 主体完成；真走通差 §E 容器写实现（队列已通）。Cards 载荷桥接、Validate 崩溃
防御、同区整理决策（`SetCardOrder` 废弃交 UI）已收（09-09 复核）；余广播待填、
`SetCardOrder` 残留待删、`bIsSelected` 观察项。**

已完成：

- [x] **族必填校验 `Validate()` 已实现**：覆写 `protected virtual bool Validate() const`——
  `Instigator` 有效且带容器接口、`Targets.Num()==1` 且 `Targets[0]` 有效带容器接口、
  `Message` 有效（`Message->IsValid()`）；不满足返回 `false`（基类 `Start` 拒绝入队、
  可修复后重试）。
- [x] **双端解析已落地**：交易内 `GetInterface(AActor*)`（`.cpp` 匿名命名空间：Actor 自实现
  接口则直 `Cast`，否则 `FindComponentByInterface`）——`Instigator` → 桌面
  `ACardTableManager`、`Targets[0]` → 玩家 `UCardManagementBusComponent`。
- [x] **`Execute()` 已实现**（`.cpp`，与 `04` §4 形状一致），族侧主链已编排齐：
  `双端解析 → Select 选 Id → Consume 移出 → Add 落入 → Finish(bool)`（代码见下框）。
- [x] **`FoldMods()` 直通空实现**（移动族无折叠请求）。
- [x] **`Cards` 载荷桥接 `FArkCard`（09-09 复核）**：字段已为 `TArray<FArkCard>`；`Create`
  第二参 `CardsToMove` 预填（显式指定牌路径），`HandleMovement*` 的 `Consume` 结果回填——
  卷 `08` §7.5 收敛项达成，不再有"裸 `FGameplayTagContainer` 载荷"残留。
- [x] **`Validate` 失败日志空指针防御已落（09-09）**：无效 `Instigator`/`Targets[0]` 走
  `IsValid() ? GetName() : TEXT("nullptr")`，不再解引用崩溃（原待修缺陷已收）。
- [x] **同区短路即终态（09-09 决策）**：`From == To && Message->_From == Message->_To` 直接
  `Finish(false)`（不落子）——**服务器不承接同区改序**：`SetCardOrder` 废弃、手牌整理全交
  UI 表现层（`08` §6 原"容器内直接改序"约定已按此修订）；区数组顺序 = 服务器落子序
  （`Add` 两跳），玩家手牌呈现序由 UI 自理、不影响取牌序。

待办：

- [ ] **`BroadcastFinish(bool)` 族广播待填**：族钩子仍空（注释占位：`bSuccess=true` 时广播
  `Notify::Card::Moved`）——P2 收尾一并接或随 P3 通知通道。
- [ ] **`SetCardOrder` 残留删除（决策已定，源码未动）**：`UCardManagementBusComponent` 上
  声明 + 空实现（`CardManagementBusComponent.h/.cpp`）已无用途（同区改序交 UI），连同
  `08` §6/§10 旧"或 `SetCardOrder`"表述一并清理。
- [ ] **观察项（09-09，代码复核发现）**：`bIsSelected = Cards.IsEmpty()` 命名与分支调用方向
  疑似写反——`Cards` 空（未预选、应走 DSL）时进入 `HandleMovement_Selected`（空遍历 →
  空 `Consume`，不落子）；`Cards` 非空（已显式指定牌）反走 `HandleMovement`（DSL 全选覆盖
  预选）。已按代码现状如实记录，修否待定（见 §7 观察；修正方向：`bIsSelected = !Cards.IsEmpty()`）。

```cpp
// = 代码现状（CardMoveTransaction.cpp，09-09 复核）：Execute 主链 + 双路径
if (!Validate()) return Finish(false);              // Execute 期再校验兜底
bIsSelected = Cards.IsEmpty();                      // 观察项：命名/分支方向待核（§7）
From = GetInterface(Instigator.Get());  To = GetInterface(Targets[0].Get());
FoldMods();                                         // 直通空实现（移动族无折叠请求）
if (From == To && Message->_From == Message->_To) return Finish(false);  // 同区短路，不落子
if (bIsSelected) HandleMovement_Selected(From);     // 预选牌：GetCardsByKey 定位 → Consume
else             HandleMovement(From);              // DSL：Select 选 Id → Consume
switch (To->Add(Message->_To, Cards, *Message)) {   // 落位；Cards = Consume 回填载荷（FArkCard）
    Accepted → Finish(true);  Full/Mismatch → Finish(false); }
```

真走通仍差 §E 收口：两端接口已实现，但 `Consume` 源区删除下标错位、玩家侧 `Add`
Equipment/Judgement 不落区，且桌面写接口缺门控（09-09 复核修正，§2-E）。

### E. 两容器落子（卷 `08` §3）

**状态：⚠ 主体已实现（09-09 复核修正）；`Consume` 删位错位、玩家 `Add` Equipment/Judgement
不落区、桌面写接口缺门控，待修。**

说明（09-09 复核修正，以当日前代码为准）：六方法两端均已落实现，旧"写接口空壳"表述已失效；
玩家侧 `Consume`/`Add` **已带 `HasAuthority` 门控**、桌面侧**无**；剩余缺陷：`Consume` 从
源区删除时误用"取出副本内位置"作下标（错删/越界面，两端同构）、玩家侧 `Add`
Equipment/Judgement 分支经空实现的 `Equip`/`PutIntoJudgement` 不落区、桌面 `JudgementArea`
清单侧不一致（观察项）——细目见下与 §7；玩家侧 `SetCardOrder` 已废弃（09-09 决策，§2-D），
残留空实现随本段清理。

已实现（09-09 最新代码）：

- [x] **读侧**：玩家 `GetCardsByKey`（三区）/`GetCardById`（三区遍历 Identity 反查）/
  `GetAreaKeys`（三区 + Owner 上其他容器组件追加）已实现；桌面 `GetCardById`（五区遍历）、
  `GetAreaKeys`/`GetCardsByKey`（Pile/Discard/Cache/Used 四区）已实现。
- [x] **`Select`**（两端同构）：区域空时前置返回；按 `ORDER=Top/Bottom/Random` 扫描 +
  `Msg(Card)` 谓词只读过滤，返回源区下标（`Temp` 浅拷贝与源区同序）。
- [x] **`Add` 基本落位**：玩家 `Hand` = `Append`；桌面 Pile `Top`=`Append`（数组尾端 = 牌堆
  顶）、`Bottom`=头前置、`Random`=逐张随机插；Discard/Used/Cache/Judgement 按区 `Append`。
- [x] `Consume` 签名已去 `const` + `[[nodiscard]]`；`GetContainerActor()` 已内联
  （玩家侧返 `Owner` / 桌面侧返 `this`）；玩家三区字段与桌面五字段（含 `JudgementArea`）均已
  `DOREPLIFETIME`。

缺陷（09-09 复核修正，以当日前代码为准；待修，未改代码）：

- [ ] **`Consume` 源区删除下标错位（两端同构）**：`CardManagementBusComponent.cpp:248-277` /
  `CardTableManager.cpp:189-218`——删除循环用 `CardArea->RemoveAt(OutCards.Find(OutCard))`，
  取的是**取出副本 `OutCards` 内的位置**而非 `CardIndexes` 的源区下标：仅当传入下标恰好为
  "从头开始的升序连续前缀"（如 `Select` Bottom 升序全命中）时碰巧正确；Top 抽取（降序下标）、
  Random、乱序传入都会错删未选中牌，且源数组逐轮收缩后易 `RemoveAt` 越界。正确做法：按
  `CardIndexes` 降序删除（或先校验再删）+ `IsValidIndex` 防御。
- [ ] **玩家侧 `Add` Equipment/Judgement 分支不落区**：`CardManagementBusComponent.cpp:290-314`
  数量校验后经 `CanEquipCard`/`CanPutInJudgement`（**恒 `true` 占位**）→ `Equip`/
  `PutIntoJudgement`（**空 const 实现，不写任何区**）→ `Cards.RemoveAt(0)`——传入牌被清空但
  **未写入 `EquipmentArea`/`JudgementArea`**，`Accepted` 返回而区域未变（"移入"半跳失效 +
  牌凭空消失）；按 Tag 定槽插入与容量不变量（`08` §8 同种唯一/保序）未实现。
- [ ] **桌面侧写接口缺 `HasAuthority` 门控**：`ACardTableManager` 为复制 actor，但其 `Consume`/
  `Add`（`CardTableManager.cpp:189/220`）无 `HasAuthority` 校验——客户端调用会就地改本地副本
  （玩家侧两方法已有门控；验收 6 仅部分满足）。
- [ ] **桌面 `JudgementArea` 清单侧不一致（观察项）**：字段复制已注册（OnRep 正常触发），但
  `GetAreaKeys`/`GetCardsByKey` **不含 Judgement**，而 `Select`/`Consume`/`Add`/`GetCardById`
  会触达该区；`08` §2 判定区归玩家侧组件，桌面该字段语义/去留随 E 修复定（见 §7 观察项 ③）。
- [ ] **`Add` 未知区域键返回不一致（观察项）**：玩家侧兜底返回 `Mismatch` 且 `Cards` 未动；
  桌面侧对非 Pile/Discard/Judgement/Used/Cache 的键**仍返回 `Accepted`**（无兜底校验）。
- [ ] **`SetCardOrder` 残留删除**（09-09 决策，随本段清理）：玩家侧 .h 声明 / .cpp:118 空实现。

### F. `ResolveContainer`（卷 `08` §4）

**状态：⚠ 局部落地；统一公共入口未抽。**

- [x] **解析逻辑已落地**（09-09）：交易内局部 `GetInterface(AActor*)`
  （`CardMoveTransaction.cpp` 匿名命名空间：Actor 自实现接口直 `Cast`，否则
  `FindComponentByInterface` 取组件），供族双端解析可用——玩家区（Hand/Equipment/
  Judgement）→ Owner 的 `UCardManagementBusComponent`；桌面区 →
  `ACardTableManager::Get(World)`。
- [ ] **抽统一公共入口 `ResolveContainer`**（拟放 `BattleFunctionLibrary` 或流程子系统）：
  Owner 空归属规则在此统一。
- [ ] **类型链定稿**：以代码 `AActor*` 入参（交易经公开 `Instigator`/`Targets` 承载
  `AActor*`，原 `Init` 已删）修订卷 `08` §4 的 `APlayerState*` 草稿。

### G. `StartGame` 发牌打通（卷 `08` §11 案例）

**状态：❌ 未打通（`InitCardDeck` 空 + §E 缺陷未收——队列已通，交易出队后受 §E 缺陷影响仍不落数据）。**

已就绪（09-09 复核）：

- [x] `StartGame` 已按交易化编排——`ModeComp->AllocateIdentity()` 返回值驱动
  `InitPlayers(Start)`；选角通知后**已接入 `ModeComp->InitCardDeck()` 调用**（行 250）。
- [x] 逐玩家 `UCardMoveTransaction::Create("NUM=%d FROM=-AP TO=-AH")` → 公开字段直赋
  （`Instigator = Manager->GetContainerActor()`、`Targets.Add(Player)`）→ **`Start()`**
  （服务器门控 + `Validate` + `Started` → `EnqueueTransaction`；原 `Init`/直调 `Enqueue`
  已删净）。
- [x] 模式组件已抽象化并拆 `UDefaultModeComponent`/`UFactionsBattleModeComponent` 两子类
  （身份分配/起手数迁入实现）。

待办：

- [ ] **`InitCardDeck` 两子类覆写仍空** → 真填牌堆（当前牌堆空、无牌可摸）。
- [ ] §E 缺陷修复（`Consume` 按源下标/降序删、玩家 `Add` 真落 Equipment/Judgement、桌面写
  接口补门控）→ 交易可出队落子、发牌可观察（发牌验收项 §4.5）。

卡点：§E 接口已实现但有缺陷（`Consume` 源区删除下标错位 / 玩家 `Add` Equipment·Judgement
不落区 / 桌面写接口缺门控）→ 交易出队不落正确数据；发牌链 = 牌堆空（`InitCardDeck` 未填）+
落子缺陷，实际未落子（09-09 复核修正）。

## 3. DSL 测试矩阵（卷 `08` §5；每条：解析断言 + 移动后两端数量对账 + 守恒）

| 场景 | Msg | 期望 |
|---|---|---|
| 摸牌 | `NUM=2 FROM=-AP TO=-AH` | Pile→Hand / 2 |
| 摸牌默认 | 空串 | Pile→Hand / 1 |
| 弃牌 | `NUM=1 FROM=-AH TO=-AD DISCARD=1` | Hand→Discard；`_bDiscard=true`（先修 §A） |
| 装备 | `NUM=1 FROM=-AH TO=-AE` | Hand→Equipment（槽容量 1） |
| 进/出判定区 | `FROM=-AH TO=-AJ` / `FROM=-AJ TO=-AD` | 槽进出正确 |
| 弃牌堆抽回 | `FROM=-AD TO=-AH` | Discard→Hand（强改误判已修，可跑） |
| 花色/点数过滤 | `CONDITION:SUIT=/H`、`MIN=5 MAX=9` | Predicate 命中 |
| 特殊区 | `NUM=5 FROM=-AP TO=-AS AREA=Skill.Foresee` | `AREA=` 分支（原生标签） |

## 4. 验收

1. 单步移动：调试入口发起 `UCardMoveTransaction`，日志 `[Move] From=X To=Y Num=N`，
   源区减、目标区加。
2. 全链路可见：客户端读复制区域前后数量一致。
3. DSL 回归：§3 用例表全过（含 `AREA=` 边界与 `_bDiscard`）。
4. 守恒对账：任意移动全局卡牌总数不变。
5. `StartGame` 开局后双方手牌 = 起始数、牌堆扣除正确。
6. 门控：客户端调用两容器写接口（`Consume`/`Add`）被忽略；日志英文、区域名取 `CardTags::*`。

## 5. 边界（不碰）

- ❌ 时机窗口 / `Event.Timing.Card.Move` 可干预、区域进出细分
  （查询 `Timing.Card.AreaEnter/AreaLeave`、通知 `Notify.Card.AreaEntered/AreaLeft`，
  `03` §3.1）——P3。
- ❌ 阶段体规则（摸几张/弃牌上限/判定区遍历）——后续薄层。
- ❌ 伤害/濒死/出牌响应链——P4/P5/P7。
- ❌ 客户端选牌交互与表现队列——仅消费复制。

## 6. 完工检查表

> 勾选口径与 §2 行动项一致：[x] = 已完成（09-09 复核）；[ ] = 待办。
> 主项 [x] 且其下尾项均收口 = 该节完工；各节状态与 §2 速览表一一对应。

**A / B（前置已核销）**

- [x] A：`_bDiscard` 判真与 DISCARD 值独立解析已落位（09-08：`GameMessage.cpp:159`
  键解析 `FindKeyHelper` 短路 + 独立取值；`AREA=` 走 `RequestGameplayTag` 原生标签）。
- [x] B：代码对齐表逐行核销（09-08）——Cancel/End、`Init` 删净改公开字段直赋、驱动三方法、
  P3 占位、`GetState`、`ActiveTransaction` 指针、`Consume` 去 `const` 均已对齐。
  - [ ] 尾项：`Execute` 访问层级（§2-C 决策点）+ `ResolveContainer` 类型链按 `AActor*` 定稿。

**C（基类与队列）**

- [x] 基类**五态 + 单收尾**重构落地（`Start`/`Finish(bool)`/`BroadcastFinish(bool)`，
  `Finish` 内已接 `OnTransactionFinished` 踢链）。
- [x] 收敛三件落地：`ModeRequests` + `AppendModification`（`State==Querying`）；`FoldMods`
  移动族已给直通空实现（P4 伤害族折叠）。
- [x] 队列"单条驱动/收尾踢"体已修正：§2-C 缺陷①②③消除（`Enqueue` 入队 + 忙锁排队、`Drive`
  空守卫，09-09）→ `StartGame` 双交易可串行出队。
- [ ] 决策点：`Execute` 公开 vs 改回受保护 + 增驱动入口（方案回填 `04` §7）。

**D（族实现）**

- [x] `Execute` 执行骨架（含 `HandleMovement*` 双路径 + 同区短路）+ `Validate`
  （空指针防御）+ `FoldMods` 直通（09-09 复核）。
- [x] `Cards` 载荷已桥接 `FArkCard`（`Create(Msg, CardsToMove)` 预填 / `Consume` 回填，
  `08` §7.5）。
- [ ] `BroadcastFinish` 族广播填 `Notify::Card::Moved`。
- [x] 同区整理决策：`SetCardOrder` 废弃、改序交 UI；同区短路 `Finish(false)` 即终态（09-09）。
- [ ] `SetCardOrder` 残留声明/空实现删除（源码未动，随 §E 一并清）。
- [ ] 观察项：`bIsSelected` 分支方向待核（修正方向见 §2-D，详见 §7）。
- （真走通待 §E 缺陷收口 + 门控；§C 队列已通，09-09。）

**E / F / G（落子与发牌）**

- [x] E 主体：两容器 `GetCardById/Select/Consume/Add/GetCardsByKey/GetAreaKeys` 已实现
  （09-09 复核修正）。
- [ ] E 缺陷修复：`Consume` 按源下标/降序删（两端）、玩家 `Add` Equipment/Judgement 真落位、
  桌面写接口补 `HasAuthority` 门控（细目 §2-E/§7）。
- [ ] F：`ResolveContainer` 统一公共入口（解析逻辑现为交易内 `GetInterface`，09-09；
  类型链按 `AActor*`）。
- [ ] G：模式组件 `InitCardDeck` 真填牌堆（`StartGame` 已接入调用）→ 发牌可观察（验收 4.5）。
- [ ] §3 用例全回归 + 守恒对账 + 客户端可见。
- [ ] 编译无警告；旧名删净（均已删）、`ActiveTransaction` 指针化后无 `int32` 残留。
  - 旧名清单：`ProcessTransaction`/`Init`/`GetInstigator`/`GetTarget`/`IsTransactionCanceled`/
    `NotifyTimings`/`Begin`/`Cancel`/`bBegun`/`End`。

## 7. 现存问题（持续更新）

- **主路径未通清单**：同 §1 各 ⚠ 项；当前阻塞 = §E 写接口缺陷修复（`Consume` 源区删除下标
  错位、玩家 `Add` Equipment/Judgement 不落区、桌面写接口缺门控）+
  `InitCardDeck` 空实现（见本文件 §2 E/G）；§2-C 队列驱动缺陷①②③已修正（09-09）。
- **09-09（最新，代码复核）**：
  - 已落：`UCardMoveTransaction` 族实现（`Execute` 执行骨架 + `Validate` + `FoldMods` 直通；
    **`Cards` 载荷已桥接 `FArkCard`**，`Create(Msg, CardsToMove)` 支持预选牌；
    **`Validate` 失败日志空指针防御已修**）；队列驱动三方法按修正方向收口（`Enqueue` 入队 +
    忙锁排队、`Drive` 空守卫 + `RemoveAt`、收尾踢链，缺陷①②③消除）；两容器写接口已实现
    但 `Consume`/`Add` 有缺陷（见下条 §7 最新复核）、`InitCardDeck` 仍空（细目见 §1/§2 E/G）。
  - 剩余代码缺陷待修：
    - **观察项**：`bIsSelected = Cards.IsEmpty()` 分支方向疑似写反——`Cards` 空（DSL 路径，
      如 `StartGame` 发牌 `Create("NUM=N FROM=-AP TO=-AH")`）走 `HandleMovement_Selected`
      （空遍历 → 空 `Consume`，不落子）；`Cards` 非空（显式指定牌）反走 `HandleMovement`
      （DSL 全选覆盖预选）。修否待定，修正方向：`bIsSelected = !Cards.IsEmpty()`（§2-D）。
  - **决策（09-09）**：同区整理交 UI——`SetCardOrder` 废弃（组件残留声明/空实现待删）；
    服务器不承接同区改序，`Execute` 同区短路 `Finish(false)` 即终态（`08` §6 已同步修订，
    见 §2-D/§6-D）。
  - 另：基类 `Execute` 现为 `public`（驱动器直调可行），与卷 `04`"族钩子受保护"目标有出入
    ——去留随 §2-C 决策点定；`BroadcastFinish(bool)` 族广播空实现未填
    （`Notify::Card::Moved` 等通知待接）。
- **09-09 复核修正（两容器落子 §E，以当日前代码为准）**：`GetCardById`/`Select`/`Consume`/
  `Add`/`GetCardsByKey`/`GetAreaKeys` 两端均已落实现（玩家 `CardManagementBusComponent.cpp` /
  桌面 `CardTableManager.cpp`）——旧"写接口空壳/未开工"状态作废；并**更正上版误记**：
  桌面 `Add` Pile Top 实为 `PileArea.Append`（非覆盖）、桌面 `JudgementArea` 复制已注册、
  玩家写接口已带 `HasAuthority` 门控、`Select` 空区有前置守卫。复核保留缺陷与观察项
  （修复方向见 §2-E）：
  - ① **`Consume` 源区删除下标错位（两端同构）**：`CardArea->RemoveAt(OutCards.Find(...))`
    用取出副本内位置作删除下标——Top/乱序选取会错删、数组收缩后 `RemoveAt` 越界（玩家侧有
    `INDEX_NONE` 判断、桌面侧无；均无 `IsValidIndex`）；应源下标降序删 + 越界防御。
  - ② **玩家 `Add` Equipment/Judgement 分支不落区**：`CanEquipCard`/`CanPutInJudgement` 恒
    `true` 占位、`Equip`/`PutIntoJudgement` 为空 const 实现，`Cards.RemoveAt(0)` 清参后区域
    未写入（`Accepted` 而区未变，牌消失）；`08` §8 不变量未实现。
  - ③ **桌面 `JudgementArea` 清单侧不一致（观察项）**：字段复制已注册、`Select`/`Consume`/
    `Add`/`GetCardById` 触达，但 `GetAreaKeys`/`GetCardsByKey` 不含；`08` §2 判定区归玩家侧，
    桌面字段语义/去留随 §E 修复定。
  - ④ **桌面写接口缺 `HasAuthority` 门控**：`Consume`/`Add` 客户端可调改本地副本（玩家侧已
    有门控；验收 6 部分满足）。
  - ⑤ **`Add` 未知区键兜底不一致（观察项）**：玩家侧 `Mismatch`、桌面侧 `Accepted`（无兜底
    校验，桌面 `Cards` 未动但报成功）。
- **09-08 复核**：容器接口声明与交易骨架命名已对齐卷 `08` §3/`04` §2 五态
  （`MoveIn/MoveOut`、`End`、`Begin`、`Cancel`、`bBegun`、`ProcessTransaction` 等旧名零残留）；
  差异清单已并入 §1/§2-B，不另留档。
- **09-08（该时点记录，09-09 复核见上）**：
  - `_bDiscard` 缺陷、`Consume` 误标 `const`、`ActiveTransactionIndex` 未指针化三条已消除；
  - 交易类型随 `EventTypes.h`/`EventTags.h` 预置（折叠载体 `FTransactionModRequest`
    + Op 集 `EModOp{Ignore, Add, Multiply, Clamp}`、`FResponseEntry`/`FResponseWindow`
    + Timing/Notify 原生标签族）；
  - **命名已统一**：`ModeRequests`/`FTransactionModRequest` 即卷 `04` 用词（09-08 拍板），
    `ETransactionState` 类说法已不再用——状态为基类内嵌 `EState` 五态。

## 8. 设计备忘（09-09）：FArkCard 不承载归属（Owner）

- **议题**：是否给 `FArkCard` 增加 `Owner` 成员——进入玩家任意区域置当前玩家、进入弃牌区置
  `nullptr`。
- **语义确认**："牌归谁 ≠ 牌在什么容器"成立——牌被使用后（落桌面 `Used` 缓冲堆）至真弃置
  前，仍应视为打出它的玩家的（`08` §7.6：`Used` 不算重置位，入 `Discard` 才终结）。
- **结论（09-09 拍板）：引擎与数据结构不改**——`FArkCard` 不加 `Owner` 成员、区域条目不包
  `{Card, Owner}`；需要该语义的**技能自行实现记录**。
  - 归属在玩家区 / 弃牌堆可由"所在容器宿主"推导（玩家区 = 持有者，`GetContainerActor`；
    弃牌堆无主 = 桌面容器），引擎已有单点（`08` §4）；
  - `FArkCard` 为值类型、区条目 / 交易载荷多拷贝，冗余归属位会引入陈旧副本风险，且易主 /
    入弃的写入点会散落各效果；
  - "Used 期仍归打出者"属技能语义需求（如复活 / 回手还给出牌者、使用痕迹只随入弃清理）——
    由需要它的技能挂实例标记 / 来源自记录（`08` §7.6 终结类别、`06` 来源链），引擎只保留
    "入弃即终"的剥离位，不做全局归属特判。
- **落点**：本备忘不产生 P2 行动项（§E/F/G 不变）；若未来出现"所有者 ≠ 当前持有者"双轴
  （借调 / 驻场归原主）语义，再单独评审建模。
