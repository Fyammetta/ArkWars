# 00 实施路线图

> 总纲与对账：战斗框架按 P0~P7 分阶段施工，每个阶段一个文件（`02-阶段性任务/P{编号}-*.md`）。
> 设计与实现的唯一对账表：**任何阶段动工前先读这里与对应卷**；阶段完成后回填状态。

## 1. 阶段总览

| 阶段 | 名称 | 对应整体卷 | 依赖 | 估时 | 状态（2026-09-08） |
|---|---|---|---|---|---|
| P0 | 地基收尾 | 全卷（门控/标签/命名） | — | 已并入前期 | ✅ 主体完成 |
| P1 | 阶段状态机 | `02` | P0 | ~2~3 天 | ✅ 主体已实现；收尾 #1~#4 代码全部落位、待运行验收（发牌链路归 P2，见 P1 施工指南 §4/§7，09-08） |
| P2 | 卡牌移动交易 | `04`+`08` | P1 | ~3~4 天 | ⚠ 进行中（09-08 最新：接口扩至七方法——读 `GetCardsByKey/GetCardById/Select/GetAreaKeys` + 写 `Consume`（已去 `const`）/`Add` + `GetContainerActor`；交易骨架/解析器/只读出口就绪，`ActiveTransaction` 已指针化、`EventTypes`/`EventTags` 与基类字段预置、`_bDiscard` 已修；仍阻塞：两容器写接口空壳 + 交易队列空壳 + `InitCardDeck` 空 → 主路径未通） |
| P3 | 时机总线与响应窗口 | `03`+`10` | P2 | ~4~5 天 | 未开工 |
| P4 | 数值与伤害结算 | `06`(属性)+`04`(伤害) | P3 | ~2~3 天 | 未开工 |
| P5 | 濒死与判定流程 | `04`+`08`+阈值 | P4 | ~2~3 天 | 未开工 |
| P6 | 技能·目标·频率 | `09`+`05`+`07` | P3、P4 | ~4~5 天 | 未开工 |
| P7 | 状态效果与扩展收尾 | `06`+`10`+`11` | P6 | 持续 | 未开工 |

## 2. 依赖关系

```
P0 ─ P1 ─ P2 ─ P3 ─┬─ P4 ─ P5
                    └─ P6 ─ P7
```

- **P2 是 P3 的地基**：交易队列"单条驱动 + 收尾踢"结构先行，P3 只在 `DriveTransaction` 里把 `Execute()` 换成 `QueryTimings()`。
- **P3 是分水岭**：P3 前全同步、不接窗口；P3 后干预点全部生效。
- **P4/P6 可并行**（P4 走数值、P6 走技能面），但建议 P4 先，让 P6 的技能体有真伤害可打。

## 3. 阶段 → 卷 → 代码 对账表

| 阶段 | 主要产出（代码） | 验收主线 |
|---|---|---|
| P1 | `UBattleGameFlowSubsystem` FSM/轮转/修正队列 | 一局自动走完所有阶段并正确换人 |
| P2 | `UBattleTransaction`/`UCardMoveTransaction` + 两容器读写落子（`Consume`/`Add`，`08` §3；`MoveIn/MoveOut` 已废除）+ 队列驱动 | 全部基础移动服务器正确、客户端可见 |
| P3 | 时机标签族、开窗过程、`UAbilityTask_WaitResponses`、窗口策略、`State.Responding` 锁 | 测试技能"判定前跳过"类干预成立；交易窗口可修改 |
| P4 | `AttributeSet`(生命) + 伤害 GE + `UDamageTransaction` | 全链路走通，含"防止伤害"分支 |
| P5 | `UDyingTransaction` + 阈值驱动 + 判定流程薄层 | 濒死求回应救回/淘汰；判定牌移动对账 |
| P6 | 技能声明扩展、时机索引、`FrequencyLedger`、目标/距离 Provider、演化骨架 | 一组测试技能全机制跑通（自动/询问/限次/距离） |
| P7 | 状态运行时、持续效果、响应链完善、表现队列、回放(可选) | 复杂对局可完整走完并复现 |

## 4. 验收贯穿线（每个阶段都要能跑）

1. **阶段巡转**：一局从 `Begin` 走到下一位玩家 `Begin`，日志/复制全对。
2. **守恒对账**：全局卡牌总数不变（移动/使用守恒）。
3. **门控审计**：所有改状态入口有服务器门控；客户端零规则决策。
4. **回归**：旧功能不破（发牌、阶段委托、区域复制）。

每阶段文件 §验收 展开本线对应的具体检查。

## 5. 状态更新纪律

- 施工文件顶部"实施状态"行维护三态：`✅ 完成 / ⚠ 进行中 / 未开工`，并注明复核日期。
- 完成一个阶段：在 §检查表 打勾、回填本表状态、把遗留问题下放/归档，不在已关闭阶段里堆积开放项。
- 发现"需要改引擎/抽象形状"的问题 → 先回 `01` §6.3 评审再动工，禁止打补丁式加特判。

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-08 | 交易生命周期重构定稿（五态 + 单收尾 `Finish(bool)`；`Begin`/`Cancel`/`bBegun`/`End` 删净、被防 = `Finish(false)` + 族 `BroadcastFinish(bool)` 广播）；折叠 Op 集收敛定稿 `EModOp{Ignore, Add, Multiply, Clamp}`（减 = `Add` 负值、除 = `Multiply` 倒数）；状态 = 基类内嵌 `EState` 五态（早期"四态迁 `EventTypes.h`/`ETransactionState`"记录作废）；同步 P2~P6 施工指南与卷 `04`/`03`/`09`/`01`；P2 阻塞 = 两容器写接口空壳 + 队列驱动体（含驱动器对 `protected Execute` 访问层级）+ `InitCardDeck` 空 |
| 2026-09-07 | 依新文档体系重建路线图（旧 §12 序列 P0~P5 → 新 P0~P7）；吸收 P1/P2 施工现状复核结论；P3 前皆为同步，P3 接入干预点 |
| 2026-09-08 | P1 收尾核对：#1 末位回绕/自广播、#2 空数组守卫、#3 `GetPlayerByIndex`（空守卫+有效下标+`INDEX_NONE` 回退，委托名修正 `FGameRoundChangeDelegate`）、#4 一轮结束广播均代码落位；P1 剩余运行验收，发牌链路归 P2 |
| 2026-09-08 | 进度复核（代码最新一轮）：交易基类瘦身——四态迁 `EventTypes.h`（`ETransactionState`）、删 `Init/GetInstigator/GetTarget/IsTransactionCanceled/NotifyTimings`、`Instigator/Targets` 改公开字段、`ModeRequests`（`FTransactionModRequest`）字段预置；新增 `EventTypes.*`/`EventTags.*`（修改请求/响应窗口类型 + Timing/Notify 原生标签族）；`UCardMoveTransaction` 独立至 `Transaction/SubTransaction/` + `Create(Msg)`；模式组件抽象化并拆 `UDefaultModeComponent`/`UFactionsBattleModeComponent`（身份分配/起手数迁入，`InitCardDeck` 仍空）；队列忙锁 `ActiveTransactionIndex`→`ActiveTransaction` 指针、`InitPlayerOrder` 删除（起始位改由 `AllocateIdentity()→InitPlayers(Start)` 编排）；容器接口 `Consume` 去 `const` + 增 `GetContainerActor`；DSL `_bDiscard` 判真缺陷修复。P0 §3/P1/P2 施工指南与卷 `04` §7/`03` §8 现状同步修订；P2 阻塞仍为写接口空壳 + 队列驱动空壳 + `InitCardDeck` 空 |
