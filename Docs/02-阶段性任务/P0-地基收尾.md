# P0 施工指南：地基收尾

> 对应：旧评审《战斗流程与时机系统设计》§11 现存问题清单（已随新体系归档）的收尾归档。
> 目标：把开工前的地基问题处理完毕、去向清楚，使 P1~P7 站在干净的门控/标签/命名基线上。
> **实施状态：✅ 主体已完成（余留问题不在此堆积，按去向分派到对应阶段）。**

## 1. 范围

| 做 | 不做 |
|---|---|
| 门控/复制/静态初始化类问题的一揽子修复与归档 | 未落地的新系统（那是 P1~P7） |
| 运行期造标签等"地基性"缺陷的分派 | 具体规则逻辑 |

## 2. 修复记录（✅ 已完成，代码侧已核对）

| # | 问题 | 处置 |
|---|---|---|
| 1 | 客户端 BeginPlay 运行期 NewObject 组件 → 双份同名组件 | 加 `HasAuthority` 守卫（建议后续改默认子对象，随 P 阶段顺手清理） |
| 2 | `GamePhaseToTagMap` 缺 `GameStart` 项断言 | 原生阶段标签 + `GetPhaseTag`（含 GameStart） |
| 3 | 头文件 `const static TMap` + 静态初始化期 `RequestGameplayTag` | 全部静态标签迁移原生 GameplayTag（`ArkWarTags.*`/`ArkWarFlowTypes.*`）；映射表改函数内静态 |
| 4 | 容器只注册 `EquipmentArea`，另两区永不复制 | 补 `DOREPLIFETIME` |
| 5 | `ToString().end()` 解引用（UB） | 改 `Name[Name.Len()-1]`（两处） |
| 6 | `GetSelectedCards` 取值即清空副作用 | 拆分 `Get/Consume` |
| 7 | `ShouldCreateSubsystem` 只认 `?listen` | 改 `GetNetMode() != NM_Standalone`；权威改动门控 |
| 8 | 组件查找路径（P1 期）：`PushPhase` 摸 GameMode 落空 | 统一 `GetPhaseManager()`（走 GameState） |
| 9 | 桌面容器 5 区复制注册不全 / OnRep 串位 | 5 区全注册 + 各区域独立 OnRep |
| 10 | `UCardComponentBase::Get` 权限写反 | 改仅服务器可建 |
| 11 | `UCardManagerSubsystem` 遍历错表 | 改 `CardMapping->ForeachRow<FCardComponentMapping>` |
| 12 | `GetCards/GetCardByPredicate` 只遍历手牌 | 移除声明与定义，过滤迁 `BattleFunctionLibrary::Filter*` |
| 13 | `FCardAreaSlotFactory` 三 Create 无实现 | `.cpp` 完整实现（含 Key 构造/容量） |

## 3. 余留分派（未在这里修的，明确去向，不建死档）

> 设计定稿的**废除/废弃项**去向登记于此（已完成项见 §2 #12）：容器跨区搬移接口 `MoveIn/MoveOut` → 两跳读写 `Consume/Add`；旧卡值/指针别名方案（`FConvertedCard`/`FHandCardInfo`）→ `FArkCard` 值模型；只遍历手牌的筛选出口 `GetCards/GetCardByPredicate` → 移除并迁 `BattleFunctionLibrary::Filter*`。

| 余留项 | 去向 |
|---|---|
| `CardMsgDef` 三处运行期 `RequestGameplayTag`（特殊区域/`CLASS=`）——卡种类/技能区域开放集未合规 | 数据资产方案随 P6 技能/标签上线处理；**P2 全量 DSL 测试前先确认 `AREA=` 分支用原生标签** |
| `FMoveMessage` `_bDiscard` 判真写法（`GameMessage.cpp:159` 现为键解析 `Find(IsDiscard) && Atoi(*Temp)`：无 DISCARD 键即短路 `false`、命中后值独立读取）与 `DISCARD` 值解析缺陷 | ✅ 已随 P2 §2 A 落位（判真/取值两缺陷消除，`AREA=` 分支走 `RequestGameplayTag` 原生标签）；运行回归并入 P2 §3/§4 |
| 玩家侧容器写接口 `Consume`/`Add`/`Select`/`GetCardById`（声明已按 `08` §3 迁完并增 `GetContainerActor`、`Consume` 已去 `const`，`MoveIn/MoveOut` 无残留）空返回/恒 `Accepted`、无 `HasAuthority` 门控 | **P2 补实现 + 门控**（P2 施工指南 §2 E） |
| `ActivePlayerIndex` 末位越界、`InitPlayers` 空数组除零、`GetPlayerByIndex` 无守卫、`BroadcastRoundRefresh` 空实现 | ✅ 已随 P1 收尾 #1~#4 全部落位，剩运行验收（P1 施工指南 §6） |
| `StartGame` 发牌（已交易化编排：`ModeComp->AllocateIdentity()`→`InitPlayers(Start)`；逐玩家 `UCardMoveTransaction::Create` → 公开字段直赋（`Instigator = Manager->GetContainerActor()`、`Targets.Add(Player)`）→ `EnqueueTransaction`；不再直调写接口，无 `Init()`）但队列空壳、未真正落子 | **P2 打通队列驱动 + 容器落子后生效**（P2 施工指南 §2 C/E/G） |
| 桌面侧 `ACardTableManager` 写接口（已迁 `Consume`/`Add`，`MoveIn/MoveOut` 无残留）四方法空壳、无门控 | **P2 补实现 + 门控**（P2 施工指南 §2 E） |
| 旧卡值方案残留：`FCard`/`FConvertedCard` 别名（仅注释 `@deprecated` 标记）；`FHandCardInfo` 仍被数据导入层消费；`UCardMoveTransaction::Cards` 已桥接 `TArray<FArkCard>`（P2 §2-D）；代码 `FArkCard` 字段为 `{ Identity, Card }` 且 `==` 只比 `Identity`，与卷 `08` §7.1 定稿的 `{ Id, Tags }` / "`==` 不做只比 Id 重载"尚有差异 | **P2 结构迁移一并收口**：别名加废弃宏/删净、数据层是否接 `FArkCard` 定夺、字段名与判同语义按卷 `08` §7.1 对齐；区字段已是 `TArray<FArkCard>`（`08` §7.1/§7.5、P2 施工指南 §2 D） |

## 4. 基线规约（全 P 阶段遵守，勿回退）

- 静态标签 = 原生 GameplayTag（`.cpp` `UE_DEFINE_GAMEPLAY_TAG`），禁用静态初始化期 `RequestGameplayTag`。
- 所有改状态入口 = `IsRunningOnServer()`/`HasAuthority` 门控；客户端零决策。
- 组件/管理器找宿主统一走"子系统单一入口"（`GetPhaseManager` 模式），不散落 `FindComponentByClass`。
- 日志英文 + 类别，名称取标签/枚举值，不硬编码字符串。

## 5. 完工检查表

- [x] 上表 #1~#13 修复落地
- [x] 余留项全部有明确去向（§3）
- [x] 基线规约四条约束生效
