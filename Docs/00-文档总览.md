# ArkWars 文档

> 本文档目录是 ArkWars 项目的文档入口：整体设计、实施路线图与各阶段施工指南。
> 旧单一大文档《战斗流程与时机系统设计》及 P1/P2 施工指南已归档至 `_archive/`（保留历史，不再维护）。本目录为现行体系。

---

## 1. 文档结构

```
Docs/
├── 00-文档总览.md                ← 本文档：地图 / 阅读路径 / 变更记录
├── _archive/                     ← 历史版本（旧母文档、旧 P1/P2），只读
├── 01-整体设计/                  ← 整体架构，按主题拆成 12 卷，卷卷自洽、显式互引
│   ├── 01-总览与分层契约.md      总纲：分层、依赖方向、跨卷契约、"不针对机制"的扩展模型
│   ├── 02-回合与阶段流程.md      同步主干：回合 FSM、阶段语义、轮转、阶段修正
│   ├── 03-时机与事件模型.md      事件二分：查询时机 / 事实通知，标签体系，窗口基座
│   ├── 04-交易模型.md            结算载体：交易生命周期与家族、入队/出队/嵌套
│   ├── 05-目标与距离模型.md      目标集、合法性校验与使用门槛、距离/攻击范围接口
│   ├── 06-状态与效果模型.md      BUFF/标记/持续效果：层数、到期、来源、属性修改
│   ├── 07-频率与限制模型.md      每回合/每轮/整局计数账本：上限、修改后复用、重置
│   ├── 08-卡牌与容器模型.md      区域容器、保序、判定区收单/结算、展示/盖放、出牌素材（实体/转化/视为）、标记、效果描述符
│   ├── 09-技能模型.md            技能 = 声明 + 技能体：激活类型、时机索引、技能演化
│   ├── 10-响应与交互模型.md      响应窗口策略、顺序、抵消/强制、选择输入
│   ├── 11-网络与表现模型.md      服务器权威边界、复制策略、客户端表现队列、回放
│   └── 12-事件与委托总表.md      跨卷登记册：事件/委托清单、广播者与广播时机、订阅者与登记方式、ModRequest 通知链路、交易嵌套事件序
└── 02-阶段性任务/                ← 施工路线图 + 每阶段一份施工文件（延续 P1/P2 模式）
    ├── 00-实施路线图.md          P0~P7 总览：阶段、依赖、里程碑与验收主线
    ├── P0-地基收尾.md            开工前置：遗留问题修复、标签数据驱动合规、命名对齐
    ├── P1-阶段状态机施工指南.md
    ├── P2-卡牌移动交易施工指南.md
    ├── P3-时机总线与响应窗口施工指南.md
    ├── P4-数值与伤害结算施工指南.md
    ├── P5-濒死与判定流程施工指南.md
    ├── P6-技能·目标·频率施工指南.md
    └── P7-状态效果与扩展收尾施工指南.md
```

## 2. 阅读路径

| 你想做什么 | 先读 |
|---|---|
| 理解整体骨架 | `01-整体设计/01-总览与分层契约` → 其余卷按需 |
| 想加一个"新机制"（角色技/新牌类） | `01-总览` §6 扩展路径 → 相关卷的「扩展点」小节 |
| 开一个阶段的工 | `02-阶段性任务/00-实施路线图` → 对应 P 阶段文件 → 其引用的卷 |
| 查"谁在何时广播什么、谁订阅" | `01-整体设计/12-事件与委托总表`（事件/委托权威登记册） |
| 核对一条旧结论 | `_archive/` 内旧文件（只读参照，以现行卷为准） |

## 3. 编号与命名约定

- 卷文件固定前缀 `NN-`；阶段文件 `P{阶段}-`。
- 每卷自带：**职责边界 → 与邻卷依赖契约 → 抽象 → 流程 → 扩展点 → 案例（非框架）**。
- 阶段文件统一：**目标 / 输入(依赖已完成的零件) / 施工内容(引用卷中章节) / 验收 / 边界(本阶段不碰)**。
- 时机/事实标签、区域标签等命名规范见 `03-时机与事件模型` §3 与 `08-卡牌与容器模型` §2，跨卷复用。

## 4. 三句话总纲（整套文档的设计契约）

1. **低耦合**：任何一卷只通过**接口与标签**与外界对话，不反向依赖实现；流程引擎不认具体卡牌，结算引擎不认具体技能。
2. **高扩展**：新增"机制" = **数据组合 + 接口实现**（时机标签 × 交易类型 × 目标规则 × 频率限制 × 响应链 × 效果），不新增引擎分支。
3. **不针对机制**：具体牌/技（杀、闪、无懈、濒死求桃、判定改判……）一律只出现在各卷「案例」小节，作为**验证与数据样本**，不进架构结构；引擎只提供它们共用的抽象形状。

## 5. 术语速查

| 术语 | 含义 | 出处 |
|---|---|---|
| 事实通知 / 查询时机 | 已发生（不可否决）同步多播 / 将发生（可干预）异步窗口 | 卷 03 |
| 交易（Transaction） | 有生命周期的结算对象，是"悬置"的停泊点 | 卷 04 |
| 响应窗口 / 响应链 | 时机开放给合法响应者的询问窗口；链 = 可嵌套的抵消/回应序列 | 卷 03、10 |
| 事件与委托登记册 | 跨卷集中登记：事件名 / 广播者 / 广播时机 / 订阅者 / 登记方式；新增事件须回填 | 卷 12 |
| 通知链路 | 开窗 → 通知（`FWindowOpened` + 逐个询问）→ 响应者主动 `AppendModification` → 关窗；ModRequest 非推送 | 卷 12 §4、04 §4.1 |
| 交易嵌套 / 排队 | 插入交易两条路：排队（`EnqueueTransaction`，不打断）vs 嵌套插队（`PushNestedTransaction`，挂起当前先结算子交易）；只能在交易边界发起 | 卷 04 §3.2、12 §6 |
| 频率账本 | 每回合/每轮/整局 N 次的通用计数与上限裁决 | 卷 07 |
| 使用门槛 | 目标描述解空 ⇒ 无"使用"入口；裁决只读、无原因分类、服务器复判 | 卷 05 §3.2 |
| 转化 / 视为 | 出牌素材：转化 = 消耗一张实体牌、以其花色点数当作指定特定牌打出；视为 = 不消耗实体，直接使用/打出一张无花色点数的虚拟选定牌 | 卷 08 §7.2 |
| 卡牌标记 | 挂在牌实例上的效果来源（干员技能授予等）；典型用法是授权该牌"当作某特定牌使用/打出" | 卷 08 §7.3 |
| Tag 生命周期 | 牌上衍生标签何时终结的统一模型：终结原因收口为三类（主动移除 / 离开附加者(任意区域) / 进入弃牌堆），类别由数据声明，执行位在两跳之间 | 卷 08 §7.6 |
| 判定区收单/结算 | 同种唯一收单 + 判定阶段 LIFO 结算 + 转移顺延 | 卷 08 §8 |
| 效果描述符 | 交易/技能产出效果的数据化形态（对状态/数值的修改声明） | 卷 06、08 |
| 技能演化 | 觉醒/修改技的通用处理：修改即建立独立演化副本 | 卷 09 |
| 卡牌值类型 | 存放/移动层统一载体 `FArkCard{ Id, Tags }`：`Id` = 服务器分配实例身份（整局唯一），`Tags` = 语义标签（谓词/条件只读它） | 卷 08 §7.1 |
| 出牌素材 | 使用/打出入口拿到的"这一张"：`FCardMaterial{ Kind(实体/转化/视为), Presented }`；`Presented.Id` 指向被消耗实体（视为 = 0） | 卷 08 §7.2 |

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-10 | P2 复核归档（**骨架口径 · 九次复核 · §2-C/§2-D 收口 + 未解决项定案**）：**已落地**——① `BroadcastFinish` 族广播填实：`UCardMoveTransaction::BroadcastFinish(true)` 经 `UBattleGameFlowSubsystem::OnCardMoved` 广播 `Notify::Card::Moved`（负载 = 双端 Actor + 本次实际移动的 `Cards`；`bSuccess=false` 不落子、无对应标签故不广播）；新增 A 面委托 `FCardMovedDelegate`（`ArkWars/Battle/Toolkits/ArkWarDelegates.h`，宿主 `BattleGameFlowSubsystem.h`），已登记卷 `12` §3.1/§5.1/§9 与附录 A（现有委托 5 → **6 个**）。② **访问层级定案**（原 P2 §2-C 决策点）：`UBattleTransaction` 新增公开驱动入口 `void Drive()`（非虚，当前直调 `Execute()`），族钩子 `Execute()` 由 `public` 收回 `protected`（`UCardMoveTransaction` 同步收拢），`UBattleGameFlowSubsystem::DriveTransaction` 改调 `ActiveTransaction->Drive();`——卷 `04` §2"族钩子受保护"目标达成；`P3` 开窗链只需把 `Drive()` 体改调 `QueryTimings()`，**入口名与调用方不变**（`04` §2/§3.1/§4.1/§7/附录 与 `P3` §3.1/§4.2 检查表已同步）。③ `UCardManagerSubsystem::Deinitialize` 归零 `InitializedCardCount`（原"跨局累加"观察项消除）。④ `FArkCard::Identity` 默认值显式 `= INDEX_NONE`（空槽哨兵）。**定案忽略（用户指示）**：`PointToNumber` 死代码、两端 `Consume` 缺 `IsValidIndex`（**判定默认安全**：`Select` 返回值必落 `[0,Num)`、`HandleMovement_Selected` 用 `Area.Find` 反查，调用链产不出越界）、`Execute` 的 `switch (EAreaWriteResult)` 无 `default`（`switch` 语句不触发返回路径告警）；`InitCardDeck` 牌堆内容按用户指示**暂时忽略**（骨架链路已通——`StartGame` 已接调用、Id 分配器已落地、玩家侧解析链 ⑬ 已修；仅内容占位），`CardAreaSlot::Get*Slot` 映射表同为**卡牌内容占位**不计缺陷。**P2 §7.1 未解决问题一览表已按此重排**：待解决仅剩「引擎内回归验收（§3 用例全回归 + 移动守恒对账 + 客户端可见）」，其余全为已忽略项；同步 `P2`（§2-C/§2-D/§2-E/§7.1/§7 九次复核块）、`04`、`12`、`P3` 与路线图（P2 状态注 + 变更记录） |
| 2026-09-10 | P2 复核归档（**骨架口径 · 八次复核 · §2-F 收口 + P2 未解决问题总表**）：**已落地**——卡牌区域解析抽为**唯一公共入口** `UBattleFunctionLibrary::ResolveContainer(AActor* Owner, const FGameplayTag& Area)`（`BattleFunctionLibrary.h/.cpp`，非 `UFUNCTION`）：`Owner` 为空 → `nullptr`（无归属）、自身实现接口 → 自身（`ACardTableManager`/`ABattlePlayerState` 均如此）、否则名下首个 `ICardContainerInterface` 组件（`Area` 有效时优先命中持有该区域的组件）；`CardMoveTransaction.cpp` 的匿名命名空间局部函数 `GetInterface`（含 `:14-15` 注释）**整体删除**，`Execute`（`:25`/`:26`，传 `Message->_From`/`_To`）与 `Validate`（`:56`/`:70`，`Message` 尚未校核故传 `FGameplayTag::EmptyTag`）四处改经入口，**行为不变**（`Validate` 仍在入参阶段拒绝解析不出容器的两端）；**类型链定稿** `AActor*`（卷 `08` §4 的 `APlayerState*` 草稿已同步修订）；**⚠ 前批"疑似误伤"已修**——`BattlePlayerState.cpp:97`（原 `:100`）的 `NewObject` 第二参由接口类改回 `UCardManagementBusComponent::StaticClass()`，构造出的才是容器组件、玩家侧转发链才真正查得到容器；**§2-F 至此收口**；**新增 P2 §7.1「未解决问题一览」表**——分**待解决**（`BroadcastFinish` 广播空 = 唯一骨架阻塞、`Execute` 访问层级决策点、两端 `Consume` 缺 `IsValidIndex`（加固非可达）、`switch (EAreaWriteResult)` 无 `default` 的警告风险、§3/§4 用例回归与验收未跑、`InitializedCardCount` 不归零（观察）、`PointToNumber` 死代码（观察））与**已忽略**（`InitCardDeck` 两覆写空 + `AllocateNewCard` 无调用方（按用户指示先忽略）、`CardAreaSlot::Get*Slot` 映射表恒 `INDEX_NONE`、`FArkCard::Identity` 无默认初值、⑩ 判定槽已占静默忽略）；同步 P2 施工指南（引言状态块/§1 盘点/速览表/§2-D/§2-E/§2-F/§2-G/§7）、卷 `08` §4 与路线图（P2 状态注 + 变更记录） |
| 2026-09-10 | P2 复核归档（**骨架口径 · 七次复核 · ⑭ 收口 + ⑮ 分配器落地**）：**已修**——⑭ `ConsumeTargets`：取走有效目标后补 `SelectedPlayers.Empty();`（`CardManagementBusComponent.cpp:91`，位于 `OnPlayerSelectionChanged.Broadcast` 之后、`return MoveTemp(Arr)` 之前），与头文件"取走并清空本地待选目标集合"及 `ConsumeCards` 写法一致，重复调用不再返回同一批旧目标；**§2-E 写接口缺陷至此清零**，骨架阻塞收敛为仅 `BroadcastFinish` 空（§2-D）；**已落地**——⑮ 实牌 `FArkCard::Identity` 分配器：新增 `UCardManagerSubsystem::AllocateNewCard(Class, Suit, Point)`（`CardManagerSubsystem.h:66`、`.cpp:141-144`）= `return FArkCard{InitializedCardCount++, Tags};`，计数为私有成员 `InitializedCardCount = 0`（`.h:57`）——**全库自此有唯一赋值点**，"实牌 Id 恒 0 / 按 Id 定位退化"作废；残余（卡牌内容占位，非骨架缺陷）= `AllocateNewCard` 暂无调用方、两个 `InitCardDeck()` 覆写仍空（`DefaultModeComponent.cpp:144-147`、`FactionsBattleModeComponent.cpp:31-33`）、`FArkCard::Identity` 仍无默认初值（`ArkWarCardTypes.h:25`，`INDEX_NONE` 继续作空槽哨兵）；**观察项（新登记）**——`InitializedCardCount` 未随 `Initialize`/`Deinitialize`（`.cpp:74-77`）归零，跨局持续累加（Id 仍全局唯一，如需按局从 0 起应显式重置）；**注释对齐（同批）**——删 `BattlePlayerState.cpp:15-17`、`CardManagementBusComponent.cpp:88-89`、`BattlePlayerState.cpp:74` 三处过时 TODO（所述 ①②／"并未清空"均已不成立），`CardManagementBusComponent.h:188-191` 的 `GetAreaKeys` 文档与 `CardMoveTransaction.cpp:14-15` 的容器解析注释改写为与现码一致；同步 P2 施工指南（引言状态块/§1 盘点/速览表/§2-D/§2-E/§2-G/§4/§7）、卷 `04` §7（交易队列驱动/发牌现状）与路线图（P2 状态注 + 变更记录） |
| 2026-09-10 | P2 复核归档（**骨架口径 · 六次复核 · 玩家侧转发链收口**）：**已修**——⑬ 玩家侧转发链：`ABattlePlayerState` 六个转发方法的容器筛选入参由**组件类** `UCardManagementBusComponent::StaticClass()` 改为**接口类** `UCardContainerInterface::StaticClass()`（`BattlePlayerState.cpp:19`/`:32`/`:45`/`:57`/`:70`/`:85`），`Cast<ICardContainerInterface>(Component)` 全部补判空（`:22`/`:35`/`:48`/`:60`/`:73`；`GetAreaKeys` 由原先直接解引用改为 `:88-89` 的 `if` 分支），`Add` 未命中容器组件时改 `return EAreaWriteResult::Mismatch;`（`:78`）——玩家侧读写不再静默空转、转发落空不再被交易读作 `Accepted`，发牌链（`StartGame` Pile → 玩家 `Hand`）取得到容器；⑪ 的"玩家转发侧变体"随之并入消除，至此三处（桌面 `ACardTableManager::Add:238`、玩家组件 `CardManagementBusComponent::Add:343`、玩家转发 `BattlePlayerState.cpp:78`）无权限 / 未命中分支全部为显式 `Mismatch`；**骨架阻塞收敛为** ⑭（`ConsumeTargets` 未清 `SelectedPlayers`）+ `BroadcastFinish` 空；**待清注释（非阻塞）**——`BattlePlayerState.cpp:15-17`（转发链 TODO，所述 ①② 已修）、`CardManagementBusComponent.h:188-192`（`GetAreaKeys` 文档）、`CardMoveTransaction.cpp:14-15`（"否则退化为查组件"，现码无该分支）；**加固项**——两端 `Consume` 缺 `IsValidIndex` 防御；**观察项**——`CardManagementBusComponent.cpp:20` `PointToNumber` 死代码（全库无调用方）；**⚠ 疑似误伤（已报用户待确认，未改代码）**——`BattlePlayerState.cpp:100` 的 `NewObject` 第二参（要构造的类）被同批替换成接口类，按此构造出的不是容器组件（`Comp->RegisterComponent()` 会把接口对象当 `UActorComponent` 用），转发链虽已修好也查不到容器，应为 `UCardManagementBusComponent::StaticClass()`；同步 P2 施工指南（引言状态块/§1 盘点/速览表/§2-D/§2-E/§2-F/§2-G/§4/§6/§7）与路线图（P2 状态注 + 变更记录） |
| 2026-09-10 | P2 复核归档（**骨架口径 · 五次复核 · 换装丢旧牌收口**）：**已修**——⑯ `Equip` 换装丢旧牌：交易侧 `HandleMovement_Selected` 增守卫，源区按实体反查 `CardIds` 为空（旧牌已随覆盖出装备区）时**沿用传入 `Cards` 直送目标区**、不做源侧 `Consume`（`CardMoveTransaction.cpp:126-127`），旧装备真正追加进桌面 `PlayedArea`（`Used`），不再"`Consume` 回填空载荷 → `Add(Used, {})` 空写入返回 `Accepted` 而旧牌静默丢失"；`Equip` 内原"顺序缺陷"TODO 删除（"先覆盖槽位、后发交易"本身非缺陷：`Equip` 是交易 `Add` 的执行端，踢旧装备由容器自己发起 `Equipment → Used` 次级交易，走同一条移动链路）；⑪ 玩家组件端无权限分支（`CardManagementBusComponent.cpp:343` → `EAreaWriteResult::Mismatch`，两端齐平）；**作废**——上一版记的注释滞后（`CardTableManager.cpp:197`/`:237`、`.h:120-123`/`.h:138`、`CardManagementBusComponent.h:117`/`.cpp:120`）现存码均已对齐；**骨架阻塞收敛为** ⑬（玩家侧转发链：六处 `GetComponentsByInterface` 传**组件类** + `BattlePlayerState.cpp:87` `Cast` 未判空 + `:78` 兜底 `return {}`=`Accepted`）+ ⑭（`ConsumeTargets` 未清 `SelectedPlayers`）+ `BroadcastFinish` 空 + `GetInterface` 组件转发分支**实际未实现**（`CardMoveTransaction.cpp:14-15` 注释与 P2 §2-F 旧稿滞后）；**加固项**——两端 `Consume` 缺 `IsValidIndex` 防御；**注释滞后（仍开）**——`CardManagementBusComponent.h:188-192`（`GetAreaKeys` 文档仍写"追加 Owner 组件区域"）；**观察项（新登记）**——`CardManagementBusComponent.cpp:20` `PointToNumber` 死代码（全库无调用方，其注释指向的"P2 §7 观察项"现补录）；**语义代价须知**——⑯ 守卫不做存在性校验，预置牌若仍留在别处会被复制一份（装备顶替路径满足"覆盖即摘除"前提，其他 `Create(Msg, {...})` 调用方须自查），附带收益是该路径不经 `Consume`、玩家装备区槽位下标数组不会被反向重建压缩错位；同步 P2 施工指南（引言状态块/§1/速览表/§2-D/§2-E/§2-F/§2-G/§4/§6/§7）与路线图（P2 状态注 + 变更记录） |
| 2026-09-10 | P2 复核归档（**骨架口径 · 四次复核**）：**已修**——⑥ 桌面门控空指针（两端 `HasAuthority()`，`CardTableManager.cpp:198`/`:238`）、⑧ `SetCardOrder` 残留（全库零命中）、⑦ `CanEquipCard` 取错函数（现调 `CardAreaSlot::GetEquipmentSlot`，`CardManagementBusComponent.cpp:123`）、⑫ `GetAreaKeys` 自引用递归（`CardManagementBusComponent.cpp:182-186` 现 `return {Hand, Equipment, Judgement};`，Owner 组件递归追加已删净）、⑪ 桌面端无权限分支（`CardTableManager.cpp:238` → `Mismatch`）；**作废 / 忽略**——⑨ `Equip` 载荷**并非空串**（实为 `FString::Printf` 拼出的 `FROM=-AE TO=-AU`，上一版"空 DSL → 默认 -AP→-AH"系误读工厂入参局部变量名 `Msg`；方向亦核实无误：源 = 玩家装备区、目标 = 桌面缓冲堆），⑩ `PutIntoJudgement` 槽已占静默忽略（调用前必经 `CanPutInJudgement`，按口径不计）；**口径确认**——`CardAreaSlot::Get*Slot` 映射表、`InitCardDeck` 牌堆、实牌 `FArkCard::Identity` 分配等**卡牌内容未定案者一律作占位、不计缺陷**；**新识别 ⑯**——`Equip` "先覆盖槽位（`EquipmentArea[Index] = Card`）、后发交易"致 `HandleMovement_Selected` 按实体反查不到旧牌 → `Consume` 回填空 `Cards` → `Add(Used, {})` 空写入返回 `Accepted` → 旧装备既不在装备区也未进 Used 区（静默丢失）；**骨架阻塞收敛为** ⑯ + ⑪（仅玩家组件端 `CardManagementBusComponent.cpp:342`）+ ⑬（玩家侧转发链）+ `BroadcastFinish` 空；**注释滞后待清**——`CardTableManager.cpp:197`/`:237`、`.h:120-123`/`.h:138`、`CardManagementBusComponent.h:117`/`.cpp:120`/`.h:202-205`；同步 P2 施工指南（引言状态块/§1/速览表/§2-D/§2-E/§2-G/§6/§7）与路线图（P2 状态注）|
| 2026-09-10 | P2 进度回填（代码复核，含当日二次复核更正）：**已落**——玩家侧 `Equip`/`PutIntoJudgement` 真落区（去 `const` + 门控 + 按 `CardAreaSlot::Get*Slot` 定位写入；槽已占则交换并发 `UCardMoveTransaction`）、玩家 `Add` Equipment/Judgement 分支改判（校验不过 → `Full`，旧"不落区/牌凭空消失"消除）、桌面 `Add` 补 `Mismatch` 兜底 + `Judgement` 纳入 `GetCardsByKey`/`GetAreaKeys` + 补 `HasAuthority` 门控、`UCardMoveTransaction::Execute` 的 `bIsSelected` 修正为 `!Cards.IsEmpty()`、新增标签 `Card.Type.Judge`/`Card.Type.Equip`/`Card.Area.Deprecated` 与 `CardAreaSlot::GetJudgementSlot`/`GetEquipmentSlot`（骨架）；**二次复核更正（作废）**——`GetInterface` 已改自身优先（原"分支方向反"作废）、玩家 `Consume` 已改反向重建（原"上界误用 `OutCards.Num()` → 尾部丢失"作废）；**仍待修**——桌面 `Consume` 旧 `RemoveAt(OutCards.Find(...))` 下标错位（两端缺 `IsValidIndex`）、桌面门控 `GetOwner()->HasAuthority()` 空指针、`Get*Slot` 映射未填 + `CanEquipCard` 误调 `GetJudgementSlot`、`Equip` 换装丢旧牌、无权限 `Add` 返回 `{}`=`Accepted`、`GetAreaKeys` 自递归、玩家侧解析落到 `ABattlePlayerState` 的转发链待核、`ConsumeTargets` 未清 `SelectedPlayers`、`SetCardOrder` 残留；五源文件注记按当日前代码补齐（引擎虚函数不注）；同步 P2 施工指南（§1/§2 速览/D/E/F/G/§4/§6/§7）、路线图（P2 状态注 + 变更记录）|
| 2026-09-10 | 时间/事件模型补全：新建卷 `12-事件与委托总表`（跨卷权威登记册）——事实委托清单（广播者/广播时机/订阅者/登记方式）、三通知面（事实/查询/生命周期）、开窗通知链路（`FWindowOpened/Closed` + `FTransactionModified`，明确 ModRequest = 响应者被通知后主动 `AppendModification`）、交易与窗口事件化委托 6 个（目标形态，附录给完整定义）、交易嵌套事件序；卷 `04` 新增 §3.2「交易中插入交易（嵌套栈）」（排队 vs 插队、不变量、`Execute` 不可中断的边界约束、骨架与决策点）、§4.1 通知链路、§7 状态、附录 `FNestedTransactionFrame`；卷 `03` §2/§4/§6/§7/§8 补委托指引、通知链路、插入交易对齐与状态；卷 `10` §3/§5/§7/§9 补"响应链 ≠ 交易嵌套"边界与 ModRequest 链路；P3 施工指南补 §3.7 事件与委托落地 / §3.8 开窗通知链路 / §3.9 交易嵌套入口 + 验收 8-10 + 边界 + 检查表 + 现存问题；总览目录/术语/阅读路径同步 |
| 2026-09-10 | P2 文档按当日前代码复核修正：更正上版误记（桌面 `Add` Pile Top 实为 `Append` 非覆盖、桌面 `JudgementArea` 复制已注册、玩家写接口已带 `HasAuthority` 门控、`Select` 空区有前置守卫）；保留真实缺陷与观察项（`Consume` 源区删除下标错位、玩家 `Add` Equipment/Judgement 经空实现不落区、桌面写接口缺门控、桌面 `JudgementArea` 清单侧不一致、`Add` 未知键兜底不一致）；同步引言/§1/速览表/§2-D/§2-E/§2-G/§6/§7；`UCardManagementBusComponent` 与 `ACardTableManager` 全部方法补 javadoc 中文注释（仅注释，不改函数体） |
| 2026-09-08 | 交易生命周期重构定稿（五态 + 单收尾）：`Start`/`Finish(bool bSuccess)`/族钩子 `BroadcastFinish(bool)` 取代 `Begin`/`Cancel`/`bBegun`/`End` 双收尾——被防/无效不再是第二入口，语义并入 `Finish(false)`（不 Execute，族钩子广播防止通知）；折叠 Op 集收敛定稿 `EModOp{Ignore, Add, Multiply, Clamp}`（减 = `Add` 负值、除 = `Multiply` 倒数，无独立 Sub/Div）；状态 = 基类内嵌 `EState{Spawned, Started, Querying, Executed, Finished}` 五态（早期"四态迁 `EventTypes.h`/`ETransactionState`"记录作废）。同步：卷 `04` §2/§4/§7/附录、`03`/`09` 样例、`01` 总览契约；施工指南 P2（§1/§2 现状重写、清除"命名待统一"）、P3（窗口收敛 = `Execute`/`Finish(false)`）、P4/P5/P6（`Finish(bool)` 收尾 + 新 Op 集测试项）；遗留：驱动器对 `protected Execute` 的访问层级待 P2 填驱动体时定（P2 §2-C 决策点） |
| 2026-09-08 | 进度回填（代码最新一轮）：交易基类瘦身对齐 `EventTypes.h`（`ETransactionState` + `ETransactionModOp`/`FTransactionModRequest`/`FResponseEntry`/`FResponseWindow` 类型预置）、`EventTags.*` 时机/通知标签族落地、`UCardMoveTransaction` 独立 + `Create`、模式组件拆 `Default`/`Factions`（身份/起手数迁入、基类抽象）、`ActiveTransaction` 指针忙锁、`InitPlayerOrder` 删除、`Consume` 去 `const` + `GetContainerActor` 入接口、DSL `_bDiscard` 修复；同步修订 P0 §3 / P1 / P2 施工指南、卷 `04` §7 / `03` §8、路线图状态与日志 |
| 2026-09-08 | 文档对账清理：P0/P1/P2 施工指南与卷 `04` §7"现状"表述按真实代码重写——清出 `MoveIn/MoveOut`、`End`、`ProcessTransaction`、`StartGame 直调 MoveOut` 等已废弃旧名残留（接口实为读 `GetCardsByKey`/`GetCardById`/`Select`/`GetAreaKeys` + 写 `Consume`/`Add`）；补记代码新形态（`Consume` 误标 `const`、`UCardMoveTransaction` 独立文件 + `Cards` 裸标签残留、交易基类含 `Cancel`/`GetState`/P3 占位、发牌已走 `Create→Init→Enqueue`、`FCardAreaSlot` 零使用方、`AActor*` 类型链、`FArkCard{ Identity, Card }` 与卷 `08` §7.1 命名/判同偏差）；卷 `03` §4 移动通知细分命名统一为 `AreaEntered/AreaLeft` |
| 2026-09-07 | 归档旧三份文档，重建为「11 卷整体设计 + 路线图 + 分阶段施工」文档集；引入目标距离/状态效果/频率限制等此前缺失的通用模型，全文落实"不针对机制"原则 |
| 2026-09-07 | 判定区定型：收单（同种唯一 + 转移顺延、放置无失败）与结算（判定阶段 LIFO、保进场时序、非转移判定牌入弃牌堆并以判定者广播弃牌事件）入卷 `08` §8；使用合法性门槛（目标可解性 = 有合法目标才可用、按种类/技能声明、只读状态、无注入钩子、bool 无原因）入卷 `05` §3.2；伤害纯事件与元素损伤条外置注释入卷 `06`；P5 判定阶段体对齐 LIFO 与不变量 |
| 2026-09-07 | 出牌素材三类（实体 / 转化 / 视为）与卡牌实例标记入卷 `08` §7：转化 = 消耗实体、产物花色点数与原牌相同，目标与门槛由内容声明；视为 = 无花色点数的虚拟选定牌、不消耗实体；标记不进身份标签，授权"当作某特定牌使用/打出"（共振 → 共振装置仅作内容样本，不进引擎）；使用类交易载荷同步更新（`04`） |
| 2026-09-07 | `08` §7 收口"使用入口落地分工备忘"：素材载荷（形态 + 实体 id + 目标种类键 + 来源链）、合法化先于执行（在场校验 / `GrantedUse` 授权声明 / 频率 / 使用门槛）、执行走移动交易并按目标种类取数据、标记 v1 = 实例容器上的 `Card.Mark.*` 扩展标签；标记与卡/玩家标签统一模型待评审 |
| 2026-09-07 | 卡牌值类型定稿：存放/移动层统一 `FArkCard{ Id, Tags }` 取代裸容器（`Id` = 服务器分配实例身份、整局唯一；弃 `FConvertedCard` 指针别名）；出牌素材定稿 `FCardMaterial{ Kind, Presented }`——转化产物 `Presented.Id = 原实体`、不改写存储态，视为无实体（`Id = 0`）；容器接口签名与复制演进（`FFastArraySerializer`、`ItemID = Card.Id`）及迁移时序入卷 `08` §3/§4/§7、`04` §4 |
| 2026-09-07 | 容器移动接口定稿：**废除跨区搬移专用 `MoveIn`/`MoveOut`**，容器只读写自己名下区域（只读 `GetCardsByKey`/`GetCardById`/`GetAreaKeys` + 两跳写 `Consume`（源按 Id 整壳取出）/`Add`（目标按 `_Order` 落位），`Add` 携 `EAreaWriteResult` 返回判定区拒收原因）；跨区移动统一为交易/管理器编排两跳（源 `Consume` 出 → 目标 `Add` 入）。同步 `08` §3/§6、`04` §4/§7、`01` 类表、路线图 P2 目标 |
| 2026-09-07 | Tag 生命周期入卷 `08` §7.6：终结原因收口三类（主动移除 / 离开附加者(任意区域) / 进入弃牌堆），类别由标签数据声明、引擎实现剥离点，执行位在移动编排两跳之间，不做按区域枚举的特判；`08` §7.3 标记的清除规则指向此统一模型 |
| 2026-09-07 | `P0-地基收尾` §3 补齐废除/废弃项分派：旧卡值方案（`FConvertedCard` 弱指针别名 / `FHandCardInfo` 原指针承载 / 裸 `FGameplayTagContainer` 载荷，`08` §7.1 废弃）→ `FArkCard{ Id, Tags }` 迁移；桌面侧 `ACardTableManager` `MoveIn/MoveOut` → `Consume`/`Add`（与玩家侧、`StartGame` 发牌并列登记去向）；引言列出三类废除项（容器跨区搬移接口 / 旧卡值方案 / 只遍历手牌的筛选出口），去向均指向 P2 |
| 2026-09-07 | `Consume` 返回值强制使用：签名标 `UE_NODISCARD`，取出即移出源区、返回值必须原样交给下一跳（`Add`/消耗结算去向），禁止丢弃破坏全局守恒——入卷 `08` §3（签名 + 铁律）、P2 施工指南 D 步两跳落子 |
| 2026-09-07 | 卡牌可被无效化定稿（样板：A 角色技令一名玩家下一张使用/打出的牌无效，处置档位 = 吞效果）：出牌分「使用 `Use` / 打出 `Respond`」两动作族、各开各窗（`03` §3.1 标签族 + §6 对齐表）；使用类交易新增结果位 `bNullified`——动作与移动照常（`Consume→Add` 守恒、频率账本照记），仅效果段跳过并广播 `Notify.PlayCard.Nullified`，与 `Cancel`（止动作、回滚）语义分档（`04` §4、`07` §2/§3/§7/§8、`10` §6/§7、P6 §3）；拦截实现 = 普通一次性状态（层数 1）+ 技能体监听两族时机标位并移除状态，作用域由数据声明，引擎零新增特判（`06` §10、`09` §11 样板） |
